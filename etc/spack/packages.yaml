packages:
  all:
    target: [broadwell]
    #permissions:
    #  write: group
    #  group: spack
  openssh:
    permissions:
      write: user
  openssl:
    externals:
    - spec: openssl@1.0.2k-fips
      prefix: /usr
